package com.example.userkeycloak.service;

import com.example.userkeycloak.entity.user;
import com.example.userkeycloak.repository.userRepository;
import org.springframework.stereotype.Service;
import reactor.core.publisher.Flux;

@Service
public class userService {
/*
    private final userRepository userRepository;

    public userService(com.example.userkeycloak.repository.userRepository userRepository) {
        this.userRepository = userRepository;
    }

    public Flux<user> findAll(){
        return userRepository.findAll();
    }


 */
}
